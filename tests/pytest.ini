[pytest]
testpaths = tests
markers =
    external: requires live internet access to external data-source APIs
    db: requires DATABASE_URL to be set in .env.local
    api: requires NEXT_PUBLIC_SUPABASE_URL + key to be set
timeout = 120
addopts = -v --tb=short
